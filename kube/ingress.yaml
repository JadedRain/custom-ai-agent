apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-agent-frontend-ingress
  namespace: logan-final
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - jaded-agent.duckdns.org
    secretName: ai-agent-frontend-tls
  rules:
  - host: jaded-agent.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ai-agent-frontend-service
            port:
              number: 5665
