apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-agent-api-ingress
  namespace: logan-final
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - jaded-agent.duckdns.org
    secretName: ai-agent-api-tls
  rules:
  - host: jaded-agent.duckdns.org
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: ai-agent-api-service
            port:
              number: 5000
