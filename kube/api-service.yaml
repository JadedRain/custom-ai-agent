apiVersion: v1
kind: Service
metadata:
  name: ai-agent-api-service
  namespace: logan-final
spec:
  selector:
    app: ai-agent-api
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
  type: ClusterIP
